name: Nightly Experimental sgl-router Docker Image

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'Disabled for PPU-only fork. Manual dispatch only.'
        required: false
        type: string
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  publish:
    if: github.repository == 'sgl-project/sglang'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        run: |
          docker buildx build . -f docker/sgl-router.Dockerfile \
            --platform linux/amd64 \
            -t lmsysorg/sglang-staging:experimental-router-dev \
            --push
