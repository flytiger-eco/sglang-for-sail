name: Nightly Test (PPU)

# DISPATCH SHIM -- PROBE BRANCH ONLY. Never merge this branch.
on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Full 40-character source revision to test.'
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: shim-ppu-pd-glm-mxfp4-${{ inputs.ref }}
  cancel-in-progress: false

jobs:
  pd-glm-mxfp4:
    uses: ./.github/workflows/test-ppu-pd-perf-k8s.yml
    with:
      ref: "${{ inputs.ref }}"
      pd_config: mxfp4-fp8
    secrets: inherit
