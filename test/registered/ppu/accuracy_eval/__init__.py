"""Accuracy benchmark entries for PPU CI.

The scores here come from EvalScope against public academic splits, which is
what makes them comparable to a published number; the Answer suites next door
grade a reviewed corpus of our own against rules of our own, which is what makes
those comparable across our releases.  Neither replaces the other.
"""
